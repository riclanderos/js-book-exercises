//Explain why this code logs '510' instead of 15.
console.log("5" + 10);
// The code will coerce the 10 number value into a string since the "5" is a string. It will then concatenate (join) the two values into '510'.
